name: "Heal Sentinel"
description: "AI code reviewer that refuses praise"
inputs:
  severity_floor:
    description: "medium | high | critical"
    default: "medium"
  block_merge_on_critical:
    description: "true | false"
    default: "true"
  llm_provider:
    description: "openrouter | anthropic | auto (default: auto — chooses openrouter if OPENROUTER_API_KEY set, else anthropic if ANTHROPIC_API_KEY set, else catalog fallback)"
    default: "auto"
  llm_model:
    description: "Model override (e.g. deepseek/deepseek-v4-pro for openrouter, claude-sonnet-4-20250514 for anthropic). Optional; each provider has a working default."
    default: ""
  llm_api_key:
    description: "Anthropic API key (kept for back-compat; only used when llm_provider=anthropic or auto with no OPENROUTER_API_KEY)"
    required: false
    default: ""
  openrouter_api_key:
    description: "OpenRouter API key (used when llm_provider=openrouter or auto with the key set)"
    required: false
    default: ""
runs:
  using: "composite"
  steps:
    - name: Compute diff
      shell: bash
      run: git diff "${{ github.event.pull_request.base.sha }}"...HEAD > /tmp/pr.diff
    - name: Install Heal Sentinel
      shell: bash
      run: npm install -g tfb-heal-sentinel
    - name: Run analysis
      shell: bash
      env:
        ANTHROPIC_API_KEY: ${{ inputs.llm_api_key }}
        OPENROUTER_API_KEY: ${{ inputs.openrouter_api_key }}
        HEAL_SENTINEL_LLM_PROVIDER: ${{ inputs.llm_provider }}
        HEAL_SENTINEL_OPENROUTER_MODEL: ${{ inputs.llm_model }}
        HEAL_SENTINEL_ANTHROPIC_MODEL: ${{ inputs.llm_model }}
      run: |
        set +e
        tfb-heal-sentinel /tmp/pr.diff --json --severity-floor "${{ inputs.severity_floor }}" > /tmp/tfb-heal-sentinel-report.json
        cli_exit=$?
        set -e
        # CLI exit codes: 0 = clean · 2 = findings-above-floor (informational,
        # not error — the PR-comment step is how findings surface to reviewers) ·
        # anything else = CLI error (bad args, malformed diff, unexpected crash).
        # Named fallback per D-EVERY-FAIL-SOFT-NAMES-ITS-FALLBACK-EXPLICITLY.
        case "$cli_exit" in
          0|2) echo "heal-sentinel CLI exit=$cli_exit (report generated; downstream steps proceed)" ;;
          *)   echo "heal-sentinel CLI exit=$cli_exit (unexpected CLI error — halting)"; exit "$cli_exit" ;;
        esac
        test -s /tmp/tfb-heal-sentinel-report.json || { echo "heal-sentinel: report file empty — halting"; exit 3; }
    - name: Post PR comment
      uses: actions/github-script@v7
      with:
        script: |
          const fs = require('fs');
          const report = JSON.parse(fs.readFileSync('/tmp/tfb-heal-sentinel-report.json', 'utf8'));
          const body = [
            `## Heal Sentinel - ${report.metrics.n_findings} finding(s), ${report.verdict}`,
            '',
            ...report.findings.map((finding) => [
              `### [${finding.severity.toUpperCase()}] [${finding.rule}] \`${finding.file}:${finding.line}\``,
              '',
              '```text',
              finding.snippet,
              '```',
              '',
              `**Doctrine:** ${finding.doctrine}`,
              '',
              `**Riverbed:** ${finding.riverbed}`,
              '',
              `**Heal:** ${finding.heal_shape}`,
              ''
            ].join('\n')),
            'To accept a bypass with a named fallback contract:',
            '',
            '```text',
            'heal-sentinel: bypass-accepted <finding-id-or-rule> reason: <one sentence>',
            '```'
          ].join('\n');
          await github.rest.issues.createComment({
            owner: context.repo.owner,
            repo: context.repo.repo,
            issue_number: context.issue.number,
            body
          });
    - name: Enforce critical findings
      shell: bash
      run: |
        node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('/tmp/tfb-heal-sentinel-report.json','utf8')); const critical=r.findings.filter(f=>f.severity==='critical'&&!f.named_fallback_accepted).length; if ('${{ inputs.block_merge_on_critical }}' === 'true' && critical) process.exit(1)"
