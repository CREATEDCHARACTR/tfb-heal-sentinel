# AGENTS.md

Read `skill/SKILL.md` before changing review behavior.
Run `npm run verify` for read-only source or test verification.
Run `npm run verify:release` only when build and browser-demo sync writes are intended and authorized.
Keep release claims bound to matching release receipts.
